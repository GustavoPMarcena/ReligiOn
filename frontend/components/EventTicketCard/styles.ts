import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    alignItems: "center",
    marginVertical: "4%",
  },

  row: {
    flexDirection: "row",
    alignItems: "stretch",
  },

  ticket: {
    width: 55,
    borderRadius: 18,
  },
});

export default styles;