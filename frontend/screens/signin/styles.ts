import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'column',
        gap: 10,
        marginBottom: 20
    },
    logo: {
        marginTop: 40,
        marginBottom: 40,
        alignSelf:'center'
    }
});